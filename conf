# grammar language identifier
#ATSP_LANG=

# absolute path or relative to grammar directory
ATSP_EMSDK=../emsdk

# affects tree-sitter generate subcommand
ATSP_ABI=13

# optional, defaults to tree-sitter if not specified
#ATSP_TS_PATH=tree-sitter

# output verbosity
#ATSP_VERBOSE=1
